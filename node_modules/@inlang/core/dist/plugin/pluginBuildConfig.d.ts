import type { BuildOptions } from "esbuild";
/**
 * These properties are defined by inlang and should not be overwritten by the user.
 */
declare const propertiesDefinedByInlang: readonly ["bundle", "platform", "format", "target"];
/**
 * Use this function to configure the esbuild options for plugins.
 *
 * @example
 *   import { build } from "esbuild"
 *   import { pluginBuildConfig } from "@inlang/core/utilities"
 *
 *   await build(pluginBuildConfig({
 *     // your build options
 *   }))
 */
export declare function pluginBuildConfig(options: Omit<BuildOptions, (typeof propertiesDefinedByInlang)[number]>): Promise<BuildOptions>;
export {};
//# sourceMappingURL=pluginBuildConfig.d.ts.map