// node_modules/@inlang/sdk-js/dist/adapter-sveltekit/runtime/shared/utils.js
var inlangSymbol = Symbol.for("inlang");
var addRuntimeToData = (data, runtime) => ({ ...data || {}, [inlangSymbol]: runtime });
var getRuntimeFromData = (data) => data[inlangSymbol];
var addRuntimePromiseToEvent = (event, runtimePromise) => {
  ;
  event.params[inlangSymbol] = runtimePromise;
  return event;
};
var getRuntimePromiseFromEvent = (event) => event.params[inlangSymbol];
var replaceLanguageInUrl = (url, language) => new URL(`${url.origin}${replaceLanguageInSlug(url.pathname, language)}${url.search}${url.hash}`);
var replaceLanguageInSlug = (pathname, language) => {
  const [_, __, ...path] = pathname.split("/");
  return `/${language}${path.length ? `/${path.join("/")}` : ""}`;
};
var wait = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

export {
  inlangSymbol,
  addRuntimeToData,
  getRuntimeFromData,
  addRuntimePromiseToEvent,
  getRuntimePromiseFromEvent,
  replaceLanguageInUrl,
  wait
};
//# sourceMappingURL=chunk-ULCBW442.js.map
